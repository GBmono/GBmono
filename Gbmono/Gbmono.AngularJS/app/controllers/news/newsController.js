/*
    促销信息 controller
*/
(function (module) {
    // inject the controller params
    ctrl.$inject = ['$scope'];

    // create controller
    module.controller('newsController', ctrl);

    // controller body
    function ctrl($scope) {
                
    }
})(angular.module('gbmono'));